services:
  - type: web
    name: expair
    env: node
    plan: free
    region: oregon
    buildCommand: npm ci && npm run build
    startCommand: npm run start
    autoDeploy: true
    envVars:
      - key: NODE_VERSION
        value: 20
      # Uncomment and set these when you have the values
      # - key: NEXTAUTH_URL
      #   value: https://expair.onrender.com
      # - key: NEXTAUTH_SECRET
      #   generateValue: true
      # - key: NEXT_PUBLIC_SUPABASE_URL
      #   value: https://YOUR_PROJECT.supabase.co
      # - key: NEXT_PUBLIC_SUPABASE_ANON_KEY
      #   value: YOUR_SUPABASE_ANON_KEY

